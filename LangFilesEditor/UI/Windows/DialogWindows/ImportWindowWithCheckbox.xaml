<Window x:Class="LangFilesEditor.UI.Windows.DialogWindows.ImportWindowWithCheckbox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        Title="Import"
        Width="800"
        Height="800"
        MinWidth="300"
        MinHeight="300"
        Background="{DynamicResource WindowBackgroundBrush}"
        FontFamily="{DynamicResource DefaultFont}"
        Foreground="{DynamicResource TextBrush}"
        WindowStartupLocation="CenterOwner"
        mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Name="TbNote"
                   Grid.Row="0"
                   Margin="5"
                   TextWrapping="Wrap" />

        <TextBox Name="TbText"
                 Grid.Row="1"
                 Margin="5"
                 HorizontalAlignment="Stretch"
                 VerticalAlignment="Stretch"
                 AcceptsReturn="True"
                 TextChanged="TbText_OnTextChanged"
                 TextWrapping="Wrap" />

        <CheckBox Name="CbAutoNumbering"
                  Grid.Row="2"
                  Margin="5 0"
                  Content="Автоматически нумеровать теги" />

        <StackPanel Grid.Row="3"
                    HorizontalAlignment="Center"
                    Orientation="Horizontal">
            <Button Name="BtAccept"
                    Margin="4"
                    Padding="8 4"
                    Click="Accept_OnClick"
                    Content="Accept"
                    IsEnabled="False" />
            <Button Name="BtCancel"
                    Margin="4"
                    Padding="8 4"
                    Click="BtCancel_OnClick"
                    Content="Cancel"
                    Style="{StaticResource TransparentButton}" />
        </StackPanel>
    </Grid>
</Window>